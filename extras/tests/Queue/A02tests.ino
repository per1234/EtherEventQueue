void runTests() {
  // queue() overload tests:
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_char_array, payload_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_char_array, payload_const_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_char_array, payload_byte);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_char_array, payload_int8_t);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_char_array, payload_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_char_array, payload_const_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_char_array, payload_unsigned_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_char_array, payload_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_char_array, payload_unsigned_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_char_array, payload_float);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_char_array, payload_double);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_char_array, payload_IPAddress);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_char_array, payload_String);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_char_array, payload_string_literal);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_char_array, payload___FlashStringHelper);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_char_array, payload_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_char_array, payload_const_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_char_array, payload_byte);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_char_array, payload_int8_t);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_char_array, payload_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_char_array, payload_const_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_char_array, payload_unsigned_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_char_array, payload_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_char_array, payload_unsigned_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_char_array, payload_float);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_char_array, payload_double);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_char_array, payload_IPAddress);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_char_array, payload_String);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_char_array, payload_string_literal);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_char_array, payload___FlashStringHelper);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_byte);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_byte, payload_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_byte, payload_const_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_byte, payload_byte);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_byte, payload_int8_t);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_byte, payload_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_byte, payload_const_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_byte, payload_unsigned_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_byte, payload_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_byte, payload_unsigned_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_byte, payload_float);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_byte, payload_double);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_byte, payload_IPAddress);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_byte, payload_String);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_byte, payload_string_literal);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_byte, payload___FlashStringHelper);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int8_t);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int8_t, payload_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int8_t, payload_const_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int8_t, payload_byte);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int8_t, payload_int8_t);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int8_t, payload_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int8_t, payload_const_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int8_t, payload_unsigned_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int8_t, payload_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int8_t, payload_unsigned_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int8_t, payload_float);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int8_t, payload_double);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int8_t, payload_IPAddress);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int8_t, payload_String);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int8_t, payload_string_literal);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int8_t, payload___FlashStringHelper);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int, payload_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int, payload_const_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int, payload_byte);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int, payload_int8_t);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int, payload_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int, payload_const_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int, payload_unsigned_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int, payload_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int, payload_unsigned_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int, payload_float);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int, payload_double);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int, payload_IPAddress);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int, payload_String);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int, payload_string_literal);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_int, payload___FlashStringHelper);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_int, payload_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_int, payload_const_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_int, payload_byte);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_int, payload_int8_t);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_int, payload_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_int, payload_const_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_int, payload_unsigned_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_int, payload_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_int, payload_unsigned_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_int, payload_float);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_int, payload_double);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_int, payload_IPAddress);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_int, payload_String);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_int, payload_string_literal);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_const_int, payload___FlashStringHelper);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_int, payload_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_int, payload_const_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_int, payload_byte);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_int, payload_int8_t);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_int, payload_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_int, payload_const_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_int, payload_unsigned_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_int, payload_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_int, payload_unsigned_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_int, payload_float);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_int, payload_double);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_int, payload_IPAddress);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_int, payload_String);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_int, payload_string_literal);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_int, payload___FlashStringHelper);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_long, payload_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_long, payload_const_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_long, payload_byte);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_long, payload_int8_t);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_long, payload_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_long, payload_const_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_long, payload_unsigned_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_long, payload_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_long, payload_unsigned_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_long, payload_float);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_long, payload_double);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_long, payload_IPAddress);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_long, payload_String);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_long, payload_string_literal);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_long, payload___FlashStringHelper);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_long, payload_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_long, payload_const_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_long, payload_byte);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_long, payload_int8_t);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_long, payload_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_long, payload_const_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_long, payload_unsigned_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_long, payload_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_long, payload_unsigned_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_long, payload_float);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_long, payload_double);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_long, payload_IPAddress);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_long, payload_String);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_long, payload_string_literal);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_unsigned_long, payload___FlashStringHelper);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_float);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_float, payload_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_float, payload_const_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_float, payload_byte);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_float, payload_int8_t);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_float, payload_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_float, payload_const_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_float, payload_unsigned_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_float, payload_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_float, payload_unsigned_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_float, payload_float);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_float, payload_double);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_float, payload_IPAddress);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_float, payload_String);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_float, payload_string_literal);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_float, payload___FlashStringHelper);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_double);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_double, payload_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_double, payload_const_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_double, payload_byte);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_double, payload_int8_t);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_double, payload_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_double, payload_const_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_double, payload_unsigned_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_double, payload_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_double, payload_unsigned_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_double, payload_float);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_double, payload_double);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_double, payload_IPAddress);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_double, payload_String);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_double, payload_string_literal);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_double, payload___FlashStringHelper);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_IPAddress);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_IPAddress, payload_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_IPAddress, payload_const_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_IPAddress, payload_byte);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_IPAddress, payload_int8_t);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_IPAddress, payload_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_IPAddress, payload_const_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_IPAddress, payload_unsigned_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_IPAddress, payload_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_IPAddress, payload_unsigned_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_IPAddress, payload_float);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_IPAddress, payload_double);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_IPAddress, payload_IPAddress);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_IPAddress, payload_String);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_IPAddress, payload_string_literal);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_IPAddress, payload___FlashStringHelper);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_String);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_String, payload_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_String, payload_const_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_String, payload_byte);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_String, payload_int8_t);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_String, payload_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_String, payload_const_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_String, payload_unsigned_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_String, payload_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_String, payload_unsigned_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_String, payload_float);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_String, payload_double);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_String, payload_IPAddress);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_String, payload_String);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_String, payload_string_literal);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_String, payload___FlashStringHelper);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_string_literal);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_string_literal, payload_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_string_literal, payload_const_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_string_literal, payload_byte);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_string_literal, payload_int8_t);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_string_literal, payload_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_string_literal, payload_const_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_string_literal, payload_unsigned_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_string_literal, payload_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_string_literal, payload_unsigned_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_string_literal, payload_float);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_string_literal, payload_double);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_string_literal, payload_IPAddress);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_string_literal, payload_String);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_string_literal, payload_string_literal);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event_string_literal, payload___FlashStringHelper);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event___FlashStringHelper);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event___FlashStringHelper, payload_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event___FlashStringHelper, payload_const_char_array);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event___FlashStringHelper, payload_byte);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event___FlashStringHelper, payload_int8_t);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event___FlashStringHelper, payload_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event___FlashStringHelper, payload_const_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event___FlashStringHelper, payload_unsigned_int);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event___FlashStringHelper, payload_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event___FlashStringHelper, payload_unsigned_long);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event___FlashStringHelper, payload_float);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event___FlashStringHelper, payload_double);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event___FlashStringHelper, payload_IPAddress);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event___FlashStringHelper, payload_String);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event___FlashStringHelper, payload_string_literal);
  EtherEventQueue.queue(target_IPAddress, sendPort, eventType, event___FlashStringHelper, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_char_array, payload_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_char_array, payload_const_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_char_array, payload_byte);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_char_array, payload_int8_t);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_char_array, payload_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_char_array, payload_const_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_char_array, payload_unsigned_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_char_array, payload_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_char_array, payload_unsigned_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_char_array, payload_float);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_char_array, payload_double);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_char_array, payload_IPAddress);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_char_array, payload_String);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_char_array, payload_string_literal);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_char_array, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_char_array, payload_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_char_array, payload_const_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_char_array, payload_byte);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_char_array, payload_int8_t);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_char_array, payload_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_char_array, payload_const_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_char_array, payload_unsigned_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_char_array, payload_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_char_array, payload_unsigned_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_char_array, payload_float);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_char_array, payload_double);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_char_array, payload_IPAddress);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_char_array, payload_String);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_char_array, payload_string_literal);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_char_array, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_byte);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_byte, payload_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_byte, payload_const_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_byte, payload_byte);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_byte, payload_int8_t);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_byte, payload_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_byte, payload_const_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_byte, payload_unsigned_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_byte, payload_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_byte, payload_unsigned_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_byte, payload_float);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_byte, payload_double);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_byte, payload_IPAddress);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_byte, payload_String);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_byte, payload_string_literal);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_byte, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int8_t);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int8_t, payload_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int8_t, payload_const_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int8_t, payload_byte);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int8_t, payload_int8_t);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int8_t, payload_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int8_t, payload_const_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int8_t, payload_unsigned_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int8_t, payload_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int8_t, payload_unsigned_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int8_t, payload_float);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int8_t, payload_double);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int8_t, payload_IPAddress);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int8_t, payload_String);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int8_t, payload_string_literal);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int8_t, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int, payload_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int, payload_const_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int, payload_byte);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int, payload_int8_t);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int, payload_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int, payload_const_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int, payload_unsigned_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int, payload_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int, payload_unsigned_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int, payload_float);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int, payload_double);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int, payload_IPAddress);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int, payload_String);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int, payload_string_literal);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_int, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_int, payload_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_int, payload_const_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_int, payload_byte);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_int, payload_int8_t);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_int, payload_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_int, payload_const_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_int, payload_unsigned_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_int, payload_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_int, payload_unsigned_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_int, payload_float);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_int, payload_double);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_int, payload_IPAddress);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_int, payload_String);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_int, payload_string_literal);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_const_int, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_int, payload_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_int, payload_const_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_int, payload_byte);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_int, payload_int8_t);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_int, payload_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_int, payload_const_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_int, payload_unsigned_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_int, payload_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_int, payload_unsigned_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_int, payload_float);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_int, payload_double);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_int, payload_IPAddress);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_int, payload_String);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_int, payload_string_literal);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_int, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_long, payload_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_long, payload_const_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_long, payload_byte);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_long, payload_int8_t);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_long, payload_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_long, payload_const_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_long, payload_unsigned_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_long, payload_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_long, payload_unsigned_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_long, payload_float);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_long, payload_double);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_long, payload_IPAddress);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_long, payload_String);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_long, payload_string_literal);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_long, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_long, payload_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_long, payload_const_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_long, payload_byte);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_long, payload_int8_t);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_long, payload_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_long, payload_const_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_long, payload_unsigned_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_long, payload_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_long, payload_unsigned_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_long, payload_float);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_long, payload_double);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_long, payload_IPAddress);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_long, payload_String);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_long, payload_string_literal);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_unsigned_long, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_float);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_float, payload_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_float, payload_const_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_float, payload_byte);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_float, payload_int8_t);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_float, payload_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_float, payload_const_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_float, payload_unsigned_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_float, payload_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_float, payload_unsigned_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_float, payload_float);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_float, payload_double);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_float, payload_IPAddress);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_float, payload_String);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_float, payload_string_literal);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_float, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_double);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_double, payload_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_double, payload_const_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_double, payload_byte);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_double, payload_int8_t);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_double, payload_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_double, payload_const_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_double, payload_unsigned_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_double, payload_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_double, payload_unsigned_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_double, payload_float);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_double, payload_double);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_double, payload_IPAddress);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_double, payload_String);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_double, payload_string_literal);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_double, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_IPAddress);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_IPAddress, payload_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_IPAddress, payload_const_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_IPAddress, payload_byte);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_IPAddress, payload_int8_t);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_IPAddress, payload_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_IPAddress, payload_const_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_IPAddress, payload_unsigned_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_IPAddress, payload_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_IPAddress, payload_unsigned_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_IPAddress, payload_float);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_IPAddress, payload_double);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_IPAddress, payload_IPAddress);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_IPAddress, payload_String);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_IPAddress, payload_string_literal);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_IPAddress, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_String);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_String, payload_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_String, payload_const_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_String, payload_byte);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_String, payload_int8_t);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_String, payload_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_String, payload_const_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_String, payload_unsigned_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_String, payload_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_String, payload_unsigned_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_String, payload_float);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_String, payload_double);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_String, payload_IPAddress);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_String, payload_String);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_String, payload_string_literal);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_String, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_string_literal);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_string_literal, payload_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_string_literal, payload_const_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_string_literal, payload_byte);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_string_literal, payload_int8_t);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_string_literal, payload_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_string_literal, payload_const_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_string_literal, payload_unsigned_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_string_literal, payload_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_string_literal, payload_unsigned_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_string_literal, payload_float);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_string_literal, payload_double);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_string_literal, payload_IPAddress);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_string_literal, payload_String);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_string_literal, payload_string_literal);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event_string_literal, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event___FlashStringHelper);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event___FlashStringHelper, payload_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event___FlashStringHelper, payload_const_char_array);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event___FlashStringHelper, payload_byte);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event___FlashStringHelper, payload_int8_t);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event___FlashStringHelper, payload_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event___FlashStringHelper, payload_const_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event___FlashStringHelper, payload_unsigned_int);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event___FlashStringHelper, payload_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event___FlashStringHelper, payload_unsigned_long);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event___FlashStringHelper, payload_float);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event___FlashStringHelper, payload_double);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event___FlashStringHelper, payload_IPAddress);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event___FlashStringHelper, payload_String);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event___FlashStringHelper, payload_string_literal);
  EtherEventQueue.queue(target_byte_array, sendPort, eventType, event___FlashStringHelper, payload___FlashStringHelper);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_char_array, payload_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_char_array, payload_const_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_char_array, payload_byte);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_char_array, payload_int8_t);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_char_array, payload_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_char_array, payload_const_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_char_array, payload_unsigned_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_char_array, payload_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_char_array, payload_unsigned_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_char_array, payload_float);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_char_array, payload_double);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_char_array, payload_IPAddress);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_char_array, payload_String);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_char_array, payload_string_literal);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_char_array, payload___FlashStringHelper);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_char_array, payload_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_char_array, payload_const_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_char_array, payload_byte);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_char_array, payload_int8_t);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_char_array, payload_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_char_array, payload_const_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_char_array, payload_unsigned_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_char_array, payload_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_char_array, payload_unsigned_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_char_array, payload_float);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_char_array, payload_double);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_char_array, payload_IPAddress);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_char_array, payload_String);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_char_array, payload_string_literal);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_char_array, payload___FlashStringHelper);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_byte);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_byte, payload_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_byte, payload_const_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_byte, payload_byte);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_byte, payload_int8_t);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_byte, payload_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_byte, payload_const_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_byte, payload_unsigned_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_byte, payload_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_byte, payload_unsigned_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_byte, payload_float);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_byte, payload_double);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_byte, payload_IPAddress);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_byte, payload_String);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_byte, payload_string_literal);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_byte, payload___FlashStringHelper);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int8_t);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int8_t, payload_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int8_t, payload_const_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int8_t, payload_byte);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int8_t, payload_int8_t);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int8_t, payload_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int8_t, payload_const_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int8_t, payload_unsigned_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int8_t, payload_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int8_t, payload_unsigned_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int8_t, payload_float);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int8_t, payload_double);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int8_t, payload_IPAddress);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int8_t, payload_String);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int8_t, payload_string_literal);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int8_t, payload___FlashStringHelper);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int, payload_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int, payload_const_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int, payload_byte);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int, payload_int8_t);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int, payload_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int, payload_const_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int, payload_unsigned_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int, payload_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int, payload_unsigned_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int, payload_float);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int, payload_double);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int, payload_IPAddress);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int, payload_String);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int, payload_string_literal);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_int, payload___FlashStringHelper);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_int, payload_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_int, payload_const_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_int, payload_byte);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_int, payload_int8_t);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_int, payload_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_int, payload_const_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_int, payload_unsigned_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_int, payload_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_int, payload_unsigned_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_int, payload_float);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_int, payload_double);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_int, payload_IPAddress);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_int, payload_String);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_int, payload_string_literal);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_const_int, payload___FlashStringHelper);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_int, payload_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_int, payload_const_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_int, payload_byte);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_int, payload_int8_t);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_int, payload_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_int, payload_const_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_int, payload_unsigned_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_int, payload_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_int, payload_unsigned_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_int, payload_float);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_int, payload_double);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_int, payload_IPAddress);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_int, payload_String);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_int, payload_string_literal);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_int, payload___FlashStringHelper);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_long, payload_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_long, payload_const_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_long, payload_byte);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_long, payload_int8_t);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_long, payload_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_long, payload_const_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_long, payload_unsigned_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_long, payload_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_long, payload_unsigned_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_long, payload_float);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_long, payload_double);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_long, payload_IPAddress);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_long, payload_String);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_long, payload_string_literal);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_long, payload___FlashStringHelper);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_long, payload_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_long, payload_const_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_long, payload_byte);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_long, payload_int8_t);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_long, payload_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_long, payload_const_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_long, payload_unsigned_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_long, payload_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_long, payload_unsigned_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_long, payload_float);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_long, payload_double);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_long, payload_IPAddress);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_long, payload_String);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_long, payload_string_literal);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_unsigned_long, payload___FlashStringHelper);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_float);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_float, payload_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_float, payload_const_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_float, payload_byte);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_float, payload_int8_t);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_float, payload_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_float, payload_const_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_float, payload_unsigned_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_float, payload_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_float, payload_unsigned_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_float, payload_float);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_float, payload_double);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_float, payload_IPAddress);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_float, payload_String);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_float, payload_string_literal);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_float, payload___FlashStringHelper);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_double);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_double, payload_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_double, payload_const_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_double, payload_byte);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_double, payload_int8_t);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_double, payload_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_double, payload_const_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_double, payload_unsigned_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_double, payload_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_double, payload_unsigned_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_double, payload_float);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_double, payload_double);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_double, payload_IPAddress);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_double, payload_String);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_double, payload_string_literal);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_double, payload___FlashStringHelper);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_IPAddress);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_IPAddress, payload_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_IPAddress, payload_const_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_IPAddress, payload_byte);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_IPAddress, payload_int8_t);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_IPAddress, payload_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_IPAddress, payload_const_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_IPAddress, payload_unsigned_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_IPAddress, payload_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_IPAddress, payload_unsigned_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_IPAddress, payload_float);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_IPAddress, payload_double);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_IPAddress, payload_IPAddress);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_IPAddress, payload_String);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_IPAddress, payload_string_literal);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_IPAddress, payload___FlashStringHelper);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_String);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_String, payload_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_String, payload_const_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_String, payload_byte);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_String, payload_int8_t);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_String, payload_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_String, payload_const_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_String, payload_unsigned_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_String, payload_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_String, payload_unsigned_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_String, payload_float);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_String, payload_double);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_String, payload_IPAddress);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_String, payload_String);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_String, payload_string_literal);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_String, payload___FlashStringHelper);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_string_literal);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_string_literal, payload_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_string_literal, payload_const_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_string_literal, payload_byte);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_string_literal, payload_int8_t);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_string_literal, payload_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_string_literal, payload_const_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_string_literal, payload_unsigned_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_string_literal, payload_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_string_literal, payload_unsigned_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_string_literal, payload_float);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_string_literal, payload_double);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_string_literal, payload_IPAddress);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_string_literal, payload_String);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_string_literal, payload_string_literal);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event_string_literal, payload___FlashStringHelper);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event___FlashStringHelper);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event___FlashStringHelper, payload_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event___FlashStringHelper, payload_const_char_array);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event___FlashStringHelper, payload_byte);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event___FlashStringHelper, payload_int8_t);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event___FlashStringHelper, payload_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event___FlashStringHelper, payload_const_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event___FlashStringHelper, payload_unsigned_int);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event___FlashStringHelper, payload_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event___FlashStringHelper, payload_unsigned_long);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event___FlashStringHelper, payload_float);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event___FlashStringHelper, payload_double);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event___FlashStringHelper, payload_IPAddress);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event___FlashStringHelper, payload_String);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event___FlashStringHelper, payload_string_literal);
  EtherEventQueue.queue(target_const_byte_array, sendPort, eventType, event___FlashStringHelper, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_char_array, payload_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_char_array, payload_const_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_char_array, payload_byte);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_char_array, payload_int8_t);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_char_array, payload_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_char_array, payload_const_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_char_array, payload_unsigned_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_char_array, payload_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_char_array, payload_unsigned_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_char_array, payload_float);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_char_array, payload_double);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_char_array, payload_IPAddress);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_char_array, payload_String);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_char_array, payload_string_literal);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_char_array, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_char_array, payload_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_char_array, payload_const_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_char_array, payload_byte);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_char_array, payload_int8_t);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_char_array, payload_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_char_array, payload_const_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_char_array, payload_unsigned_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_char_array, payload_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_char_array, payload_unsigned_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_char_array, payload_float);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_char_array, payload_double);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_char_array, payload_IPAddress);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_char_array, payload_String);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_char_array, payload_string_literal);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_char_array, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_byte);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_byte, payload_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_byte, payload_const_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_byte, payload_byte);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_byte, payload_int8_t);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_byte, payload_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_byte, payload_const_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_byte, payload_unsigned_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_byte, payload_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_byte, payload_unsigned_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_byte, payload_float);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_byte, payload_double);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_byte, payload_IPAddress);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_byte, payload_String);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_byte, payload_string_literal);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_byte, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int8_t);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int8_t, payload_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int8_t, payload_const_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int8_t, payload_byte);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int8_t, payload_int8_t);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int8_t, payload_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int8_t, payload_const_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int8_t, payload_unsigned_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int8_t, payload_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int8_t, payload_unsigned_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int8_t, payload_float);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int8_t, payload_double);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int8_t, payload_IPAddress);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int8_t, payload_String);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int8_t, payload_string_literal);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int8_t, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int, payload_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int, payload_const_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int, payload_byte);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int, payload_int8_t);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int, payload_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int, payload_const_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int, payload_unsigned_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int, payload_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int, payload_unsigned_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int, payload_float);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int, payload_double);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int, payload_IPAddress);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int, payload_String);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int, payload_string_literal);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_int, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_int, payload_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_int, payload_const_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_int, payload_byte);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_int, payload_int8_t);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_int, payload_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_int, payload_const_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_int, payload_unsigned_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_int, payload_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_int, payload_unsigned_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_int, payload_float);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_int, payload_double);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_int, payload_IPAddress);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_int, payload_String);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_int, payload_string_literal);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_const_int, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_int, payload_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_int, payload_const_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_int, payload_byte);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_int, payload_int8_t);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_int, payload_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_int, payload_const_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_int, payload_unsigned_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_int, payload_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_int, payload_unsigned_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_int, payload_float);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_int, payload_double);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_int, payload_IPAddress);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_int, payload_String);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_int, payload_string_literal);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_int, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_long, payload_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_long, payload_const_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_long, payload_byte);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_long, payload_int8_t);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_long, payload_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_long, payload_const_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_long, payload_unsigned_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_long, payload_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_long, payload_unsigned_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_long, payload_float);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_long, payload_double);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_long, payload_IPAddress);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_long, payload_String);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_long, payload_string_literal);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_long, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_long, payload_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_long, payload_const_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_long, payload_byte);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_long, payload_int8_t);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_long, payload_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_long, payload_const_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_long, payload_unsigned_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_long, payload_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_long, payload_unsigned_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_long, payload_float);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_long, payload_double);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_long, payload_IPAddress);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_long, payload_String);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_long, payload_string_literal);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_unsigned_long, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_float);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_float, payload_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_float, payload_const_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_float, payload_byte);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_float, payload_int8_t);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_float, payload_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_float, payload_const_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_float, payload_unsigned_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_float, payload_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_float, payload_unsigned_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_float, payload_float);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_float, payload_double);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_float, payload_IPAddress);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_float, payload_String);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_float, payload_string_literal);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_float, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_double);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_double, payload_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_double, payload_const_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_double, payload_byte);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_double, payload_int8_t);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_double, payload_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_double, payload_const_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_double, payload_unsigned_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_double, payload_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_double, payload_unsigned_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_double, payload_float);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_double, payload_double);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_double, payload_IPAddress);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_double, payload_String);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_double, payload_string_literal);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_double, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_IPAddress);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_IPAddress, payload_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_IPAddress, payload_const_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_IPAddress, payload_byte);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_IPAddress, payload_int8_t);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_IPAddress, payload_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_IPAddress, payload_const_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_IPAddress, payload_unsigned_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_IPAddress, payload_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_IPAddress, payload_unsigned_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_IPAddress, payload_float);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_IPAddress, payload_double);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_IPAddress, payload_IPAddress);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_IPAddress, payload_String);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_IPAddress, payload_string_literal);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_IPAddress, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_String);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_String, payload_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_String, payload_const_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_String, payload_byte);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_String, payload_int8_t);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_String, payload_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_String, payload_const_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_String, payload_unsigned_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_String, payload_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_String, payload_unsigned_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_String, payload_float);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_String, payload_double);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_String, payload_IPAddress);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_String, payload_String);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_String, payload_string_literal);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_String, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_string_literal);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_string_literal, payload_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_string_literal, payload_const_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_string_literal, payload_byte);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_string_literal, payload_int8_t);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_string_literal, payload_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_string_literal, payload_const_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_string_literal, payload_unsigned_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_string_literal, payload_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_string_literal, payload_unsigned_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_string_literal, payload_float);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_string_literal, payload_double);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_string_literal, payload_IPAddress);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_string_literal, payload_String);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_string_literal, payload_string_literal);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event_string_literal, payload___FlashStringHelper);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event___FlashStringHelper);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event___FlashStringHelper, payload_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event___FlashStringHelper, payload_const_char_array);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event___FlashStringHelper, payload_byte);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event___FlashStringHelper, payload_int8_t);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event___FlashStringHelper, payload_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event___FlashStringHelper, payload_const_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event___FlashStringHelper, payload_unsigned_int);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event___FlashStringHelper, payload_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event___FlashStringHelper, payload_unsigned_long);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event___FlashStringHelper, payload_float);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event___FlashStringHelper, payload_double);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event___FlashStringHelper, payload_IPAddress);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event___FlashStringHelper, payload_String);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event___FlashStringHelper, payload_string_literal);
  EtherEventQueue.queue(target_byte, sendPort, eventType, event___FlashStringHelper, payload___FlashStringHelper);
}
